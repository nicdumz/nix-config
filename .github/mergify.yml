pull_request_rules:
  - name: Automatic merge when CI passes
    conditions:
      # Note to self: applying this label requires "triage" permission at least,
      # so PRs from external contributors can't do this.
      - label="merge-queue"
      - check-success=Nix CI App
    actions:
      merge:
        method: rebase
  - name: Warn on conflicts
    conditions:
      - conflict
    actions:
      comment:
        message: "@{{author}} this pull request is now in conflict 😩"
      label:
        toggle:
          - conflict
queue_rules:
  - checks_timeout: 10 m
    merge_method: rebase
    update_method: rebase
    name: default_rule
    queue_conditions:
      - and:
          - check-success = All Garnix checks
merge_protections_settings:
  reporting_method: check-runs
merge_protections:
  # Commit Standards
  - name: Enforce conventional commit
    description: Make sure that we follow https://www.conventionalcommits.org/en/v1.0.0/
    if:
      - base = main
    success_conditions:
      - "title ~= ^(fix|feat|docs|style|refactor|perf|test|build|ci|chore|revert)(?:\\(.+\\))?:"
