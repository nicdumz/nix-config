{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build jonsnow",
            "type": "shell",
            "group": "build",
            "command": "colmena build --on=jonsnow",
            "options": {
                "cwd": "${workspaceFolder}"
            }
        },
        {
            "label": "build lethargyfamily",
            "type": "shell",
            "group": "build",
            "command": "colmena build --on=lethargyfamily"
        },
        {
            "label": "build all",
            "type": "shell",
            "group": "build",
            "command": "colmena build"
        },
        {
            "label": "build bistannix",
            "type": "shell",
            "group": "build",
            "command": "colmena build --on=bistannix"
        }
    ]
}
